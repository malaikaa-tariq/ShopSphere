# ShopSphere — submission-ready update\n\nReplace the matching files in your repository with this package. It fixes the unfinished dashboards/order UI, secure admin seeding, Stripe webhook flow, buyer orders/reviews, polished branding and responsive UI.\n\n## Run\n\nTerminal 1:\n```bash\ncd server\nnpm install\ncp .env.example .env\nnpm run seed:admin\nnpm run dev\n```\n\nTerminal 2:\n```bash\ncd client\nnpm install\ncp .env.example .env\nnpm run dev\n```\n\nClient: http://localhost:5173\nAPI: http://localhost:5000\n\n## Stripe\n```bash\nstripe listen --forward-to localhost:5000/api/payments/webhook\n```\nPut the returned `whsec_...` in `server/.env`. Use test card `4242 4242 4242 4242`.\n\n## Git cleanup\n```bash\ngit rm -r --cached client/node_modules server/node_modules 2>/dev/null || true\ngit add .\ngit commit -m "feat: finish ShopSphere marketplace submission"\ngit push origin main\n```\n
